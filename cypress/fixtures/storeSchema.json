{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "required": [
        "sold",
        "string",
        "pending",
        "available",
        "not available"
    ],
    "properties": {
        "sold": {
            "type": "integer"
        },
        "string": {
            "type": "integer"
        },
        "pending": {
            "type": "integer"
        },
        "available": {
            "type": "integer"
        },
        "not available": {
            "type": "integer"
        }
    }
}